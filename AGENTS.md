# Agent Guidelines & Repository Rules

## Versioning & Release Rule
- Always ensure that the `version` field in `package.json` (e.g. `"version": "1.0.2"`) matches the git tag (e.g. `v1.0.2`) whenever creating, tagging, or pushing releases.
- Never push mismatched versions between `package.json` and git tags.
